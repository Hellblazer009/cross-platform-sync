fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
numpy>=1.26.0
websockets>=12.0
# Optional — required for TCN residual network and training
# If not installed, edge server runs in physics-only fallback mode
torch>=2.2.0
